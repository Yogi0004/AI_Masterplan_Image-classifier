# AI_Masterplan_Image-classifier
Masterplan Image Classifier is an AI system that identifies and classifies real estate masterplan images. It detects layout elements like plots, roads, blocks, parks, and amenities. The model is trained using labeled masterplan images for accurate results.
